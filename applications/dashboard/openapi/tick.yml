openapi: 3.0.2
info:
paths:
  /tick:
    post:
      summary: Register a page view for statistical and analytical purpose.
      requestBody:
        content:
          application/json:
            schema:
              properties:
                Path:
                  description: Page path
                  minLength: 1
                  type: string
                ResolvedPath:
                  description: Fully resolved path to the application/controller/method.
                  minLength: 1
                  type: string
              required:
              type: object
        required: false
      responses:
        '201':
          description: Success
      tags:
      - Statistics

