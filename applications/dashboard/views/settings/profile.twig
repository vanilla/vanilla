{% from "@dashboard/components/macros.twig" import dashboardHeading %}

{{
    helpAsset(
        ('Heads Up!'),
        t(
            'These redirects only apply if the used tag values exist for the destination user.',
            'These redirects only apply if the used tag values exist for the destination user. '~
            'Otherwise the default Vanilla interface is shown.'
        )~
        '<br /><br />'~
        t(
            '*Allowed tags: {userID} {name} {ssoID}',
            '*Allowed tags: {userID} {name} {ssoID}'
        )
    )
}}

{{ dashboardHeading({ title: Title }) }}
{{ ConfigurationModule.render()|raw }}
