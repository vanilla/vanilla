import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { StoryContent } from "@library/storybook/StoryContent";
import { StoryTileAndTextCompact } from "@library/storybook/StoryTileAndTextCompact";
import { StoryParagraph } from "@library/storybook/StoryParagraph";
import Button from "@library/forms/Button";

<Meta title="Components/Buttons (Expiremental)" component={Button} />

# Standard Button

This is a button in it's natural environment. Notice how it entices you to press it.

## Experimental

This is an experiment with .mdx files. Refer to the other stories for Buttons instead.

<StoryTiles>
    <StoryTileAndTextCompact type="titleBar" text={"Standard"}>
        <Button>Standard</Button>
    </StoryTileAndTextCompact>
</StoryTiles>
