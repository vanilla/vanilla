import { css } from "@emotion/css";

export const AdminAssistantClasses = {
    buttonIcon: css({
        marginRight: 6,
    }),
    versionTag: css({
        marginTop: 12,
    }),
    frameBody: css({
        minHeight: "360px",
        maxHeight: "calc(100vh - 220px)",
    }),
};
